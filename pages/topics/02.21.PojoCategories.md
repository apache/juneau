---
title: "POJO Categories"
slug: PojoCategories
---

In general, Juneau allows for marshalling for a wide variety of POJO types including:

The following chart shows POJOs categorized into groups and whether they can be serialized or parsed:

## General POJO serialization/parsing support

<table className="styled w800" style={{borderCollapse: 'collapse'}}>
  <tr>
    <th>Type</th>
    <th>Description</th>
    <th>Can<br />serialize?</th>
    <th>Can<br />parse?</th>
  </tr>
  <tr className="dark bb" style={{backgroundColor: 'lightyellow'}}>
    <td colSpan={4}><b>Java primitives and primitive objects</b></td>
  </tr>
  <tr className="light bb">
    <td>
      <ul className="normal">
        <li>`String`</li>
        <li>`Integer`</li>
        <li>`Float`</li>
        <li>`Boolean`</li>
        <li>All primitive types and boxed equivalents</li>
      </ul>
    </td>
    <td>
      Standard Java primitive types (`int`, `long`, `float`, `double`, `boolean`, `byte`, `short`, `char`)
      and their boxed equivalents, plus `String`.
    </td>
    <td style={{backgroundColor: 'lightgreen', textAlign: 'center'}}><b>yes</b></td>
    <td style={{backgroundColor: 'lightgreen', textAlign: 'center'}}><b>yes</b></td>
  </tr>
  <tr className="dark bb" style={{backgroundColor: 'lightyellow'}}>
    <td colSpan={4}><b>Java Collections Framework objects, Java arrays, Java Optionals</b></td>
  </tr>
  <tr className="light bb">
    <td>
      <ul className="normal">
        <li>`HashSet&lt;String,Integer&gt;`</li>
        <li>`TreeMap&lt;Integer,Bean&gt;`</li>
        <li>`List&lt;int[][]&gt;`</li>
        <li>`Bean[]`</li>
        <li>`Optional&lt;Bean&gt;`</li>
        <li>`OptionalInt`</li>
        <li>`OptionalLong`</li>
        <li>`OptionalDouble`</li>
      </ul>
    </td>
    <td>
      <b>With standard keys/values</b><br />
      `Map` keys are primitive/string, swapped, or bean types.<br />
      `Map`, `Collection`, `Optional`, and array values are primitive/string, collection, bean, or swapped types.<br />
      `OptionalInt`, `OptionalLong`, and `OptionalDouble` are treated as first-class optional primitive types
      and serialize/parse identically to `Optional&lt;Integer&gt;`, `Optional&lt;Long&gt;`, and `Optional&lt;Double&gt;`
      respectively.
    </td>
    <td style={{backgroundColor: 'lightgreen', textAlign: 'center'}}><b>yes</b></td>
    <td style={{backgroundColor: 'lightgreen', textAlign: 'center'}}><b>yes</b></td>
  </tr>
  <tr className="light bb">
    <td>
      <ul className="normal">
        <li>`HashSet&lt;Bean,Integer&gt;`</li>
        <li>`TreeMap&lt;Integer,Reader&gt;`</li>
        <li>`Optional&lt;Reader&gt;`</li>
      </ul>
    </td>
    <td>
      <b>With non-standard keys/values</b><br />
      `Map` keys are collection, non-standard bean, raw-streamed, or otherwise non-string types.<br />
      `Map`, `Collection`, and array values are non-standard bean, raw-streamed, or toString-only types.
    </td>
    <td style={{backgroundColor: 'lightgreen', textAlign: 'center'}}><b>yes</b></td>
    <td style={{backgroundColor: 'salmon', textAlign: 'center'}}><b>no</b></td>
  </tr>
  <tr className="light bb">
    <td>
      <ul className="normal">
        <li>`Iterator&lt;String&gt;`</li>
        <li>`Stream&lt;Bean&gt;`</li>
        <li>`Iterable&lt;Integer&gt;`</li>
        <li>`Enumeration&lt;String&gt;`</li>
      </ul>
    </td>
    <td>
      <b>Streamable sequence types</b><br />
      `Iterator`, `Iterable` (non-Collection), `Enumeration`, and `java.util.stream.Stream`.<br />
      These are serialized directly as arrays. Elements are written lazily for text-based formats (JSON, XML, UON, URL Encoding, OpenAPI).
      MsgPack, HTML, and CSV may materialize to a list internally due to format constraints.<br />
      Parsing is not supported since these are inherently one-way consumption types.
    </td>
    <td style={{backgroundColor: 'lightgreen', textAlign: 'center'}}><b>yes</b></td>
    <td style={{backgroundColor: 'salmon', textAlign: 'center'}}><b>no</b></td>
  </tr>
  <tr className="light bb">
    <td>
      <ul className="normal">
        <li>`Instant`</li>
        <li>`ZonedDateTime`</li>
        <li>`Calendar`</li>
        <li>`Date`</li>
        <li>`Duration`</li>
        <li>`Period`</li>
        <li>`XMLGregorianCalendar`</li>
      </ul>
    </td>
    <td>
      <b>Date/time and duration types</b><br />
      `Calendar`, `Date`, `Temporal` subtypes (`Instant`, `ZonedDateTime`, `LocalDate`, etc.),
      `XMLGregorianCalendar`, `java.time.Duration`, and `java.time.Period`.<br />
      These are serialized and parsed natively as ISO 8601 strings without requiring swaps.
      Swap classes remain available as override mechanisms for custom formats.
    </td>
    <td style={{backgroundColor: 'lightgreen', textAlign: 'center'}}><b>yes</b></td>
    <td style={{backgroundColor: 'lightgreen', textAlign: 'center'}}><b>yes</b></td>
  </tr>
  <tr className="dark bb" style={{backgroundColor: 'lightyellow'}}>
    <td colSpan={4}><b>Java Beans</b></td>
  </tr>
  <tr className="light bb">
    <td><b>Standard beans</b></td>
    <td>
      Beans with one or more properties defined by public getters or public fields.<br />
      Properties can also be defined as final read-only fields and passed in as constructor args,
      or populated via a builder class (see Builder-based beans below).<br />
      Property values must be other standard, collection, or swapped types.
    </td>
    <td style={{backgroundColor: 'lightgreen', textAlign: 'center'}}><b>yes</b></td>
    <td style={{backgroundColor: 'lightgreen', textAlign: 'center'}}><b>yes</b></td>
  </tr>
  <tr className="light bb">
    <td><b>Non-standard or partial beans</b></td>
    <td>
      True beans that have one or more properties defined by getter and setter methods but whose property
      types include non-standard, raw-streamed, or toString-only values.<br />
      Also includes classes that look like beans but aren't: getters without setters, or classes without
      no-arg constructors and no recognized constructor-arg mapping.
    </td>
    <td style={{backgroundColor: 'lightgreen', textAlign: 'center'}}><b>yes</b></td>
    <td style={{backgroundColor: 'salmon', textAlign: 'center'}}><b>no</b></td>
  </tr>
  <tr className="light bb">
    <td><b>Virtual beans</b></td>
    <td>
      Unimplemented bean interfaces with properties of standard, collection, or swapped types.<br />
      Parsers automatically create interface proxies on top of `BeanMap` instances.
    </td>
    <td style={{backgroundColor: 'lightgreen', textAlign: 'center'}}><b>yes</b></td>
    <td style={{backgroundColor: 'lightgreen', textAlign: 'center'}}><b>yes</b></td>
  </tr>
  <tr className="light bb">
    <td><b>Read-only beans</b></td>
    <td>
      Beans with readable properties but no setters and no constructor-arg mapping.
      Equivalent to standard beans but parse-only via a swapped form is not recoverable.
    </td>
    <td style={{backgroundColor: 'lightgreen', textAlign: 'center'}}><b>yes</b></td>
    <td style={{backgroundColor: 'salmon', textAlign: 'center'}}><b>no</b></td>
  </tr>
  <tr className="light bb">
    <td><b>Java Records</b></td>
    <td>
      Java record classes (`record Foo(String bar, int baz) {}`). Record components are treated as bean
      properties. The canonical all-args constructor is used for parsing, so all record components are
      round-trip capable without any additional annotations.<br />
      The `@BeanType`, `@BeanProp`, `@MarshalledIgnore`, and `@BeanCtor` annotations are supported on record classes
      and their components.
    </td>
    <td style={{backgroundColor: 'lightgreen', textAlign: 'center'}}><b>yes</b></td>
    <td style={{backgroundColor: 'lightgreen', textAlign: 'center'}}><b>yes</b></td>
  </tr>
  <tr className="light bb">
    <td><b>Builder-based beans</b></td>
    <td>
      Classes that are constructed via a separate builder class. The framework auto-detects a builder when
      the object class has a public static `create()` method returning a builder (that implements the
      `Builder` interface), and the builder has a `build()` method returning the target type, or when the
      object class has a constructor that accepts a `Builder`-typed argument. The `@Builder` annotation
      can also explicitly name the builder class.<br />
      During parsing, the builder is instantiated, properties are populated on it, and `build()` is called
      to produce the final object.
    </td>
    <td style={{backgroundColor: 'lightgreen', textAlign: 'center'}}><b>yes</b></td>
    <td style={{backgroundColor: 'lightgreen', textAlign: 'center'}}><b>yes</b></td>
  </tr>
  <tr className="dark bb" style={{backgroundColor: 'lightyellow'}}>
    <td colSpan={4}>
      <b>Swapped objects</b><br />
      <span style={{fontWeight: 'normal', fontSize: '0.9em'}}>
        Objects that are not directly serializable but have <a href="/site/apidocs/org/apache/juneau/swap/ObjectSwap.html" target="_blank">ObjectSwaps</a> associated with them.
        The purpose of a POJO swap is to convert an object to another object that is easier to serialize and parse.
        <br /><i>Note: As of 9.5.0, date/time types (`Calendar`, `Date`, `Temporal` subtypes, `XMLGregorianCalendar`, `Duration`, `Period`)
        are handled natively and no longer require swaps. Swap classes remain available as override mechanisms.</i>
      </span>
    </td>
  </tr>
  <tr className="light bb">
    <td><b>2-way swapped</b></td>
    <td>
      Swapped to a primitive/string, standard collection, or standard bean form — reconstruction from the
      wire form is possible.
      <ul className="normal">
        <li>Custom user-defined swaps</li>
      </ul>
    </td>
    <td style={{backgroundColor: 'lightgreen', textAlign: 'center'}}><b>yes</b></td>
    <td style={{backgroundColor: 'lightgreen', textAlign: 'center'}}><b>yes</b></td>
  </tr>
  <tr className="light bb">
    <td><b>1-way swapped</b></td>
    <td>
      Swapped to a simpler form where reconstruction from the wire form is not possible.<br />
      <i>Note: As of 9.5.0, `Iterator` and `Enumeration` are handled natively and no longer require swaps.</i>
    </td>
    <td style={{backgroundColor: 'lightgreen', textAlign: 'center'}}><b>yes</b></td>
    <td style={{backgroundColor: 'salmon', textAlign: 'center'}}><b>no</b></td>
  </tr>
  <tr className="dark bb" style={{backgroundColor: 'lightyellow'}}>
    <td colSpan={4}>
      <b>`Readers` and `InputStreams`</b><br />
      <span style={{fontWeight: 'normal', fontSize: '0.9em'}}>
        Contents are serialized directly to the output stream or writer.
        Typically used for low-level language-specific replacement of POJOs using per-Media-Type POJO swaps.
      </span>
    </td>
  </tr>
  <tr className="light bb">
    <td>
      <ul className="normal">
        <li>`FileInputStream`</li>
        <li>`StringReader`</li>
      </ul>
    </td>
    <td>Raw `Reader` and `InputStream` objects. Contents are written directly; no structural wrapping.</td>
    <td style={{backgroundColor: 'lightgreen', textAlign: 'center'}}><b>yes</b></td>
    <td style={{backgroundColor: 'salmon', textAlign: 'center'}}><b>no</b></td>
  </tr>
  <tr className="dark bb" style={{backgroundColor: 'lightyellow'}}>
    <td colSpan={4}><b>Non-serializable objects with standard methods for converting to a serializable form</b></td>
  </tr>
  <tr className="light bb" style={{backgroundColor: 'lightyellow'}}>
    <td><b>2-way convertible</b></td>
    <td>
      Classes with a method that converts it to a serializable form:
      <ul>
        <li>`public X swap(MarshallingSession);` where `X` is a primitive/string, standard collection, or standard bean.</li>
        <li>`public String toString();` where the string is any meaningful data.</li>
      </ul>
      And a method that converts it back:
      <ul>
        <li>`public static T fromString(String);`</li>
        <li>`public static T valueOf(String);`</li>
        <li>`public static T parse(String);`</li>
        <li>`public static T parseString(String);`</li>
        <li>`public static T forName(String);`</li>
        <li>`public static T forString(String);`</li>
        <li>`public T(X);` where `X` is a primitive/string, standard collection, or standard bean.</li>
        <li>`public static T unswap(MarshallingSession,X);` where `X` is a primitive/string, standard collection, or standard bean.</li>
      </ul>
      Examples:
      <ul className="normal">
        <li>`java.lang.Class`</li>
        <li>`java.sql.Time`</li>
        <li>`java.sql.Timestamp`</li>
        <li>`java.text.MessageFormat`</li>
        <li>`java.text.NumberFormat`</li>
        <li>`java.util.UUID`</li>
        <li>`java.util.logging.Level`</li>
        <li>`javax.xml.bind.DatatypeConverter`</li>
      </ul>
    </td>
    <td style={{backgroundColor: 'lightgreen', textAlign: 'center'}}><b>yes</b></td>
    <td style={{backgroundColor: 'lightgreen', textAlign: 'center'}}><b>yes</b></td>
  </tr>
  <tr className="light bb" style={{backgroundColor: 'lightyellow'}}>
    <td><b>1-way convertible</b></td>
    <td>
      Classes that only have a method to convert to a serializable form:
      <ul>
        <li>`public X swap(MarshallingSession);` where `X` is any collection or bean type.</li>
        <li>`public String toString();` where the string is any meaningful data.</li>
      </ul>
    </td>
    <td style={{backgroundColor: 'lightgreen', textAlign: 'center'}}><b>yes</b></td>
    <td style={{backgroundColor: 'salmon', textAlign: 'center'}}><b>no</b></td>
  </tr>
  <tr className="dark" style={{backgroundColor: 'lightyellow'}}>
    <td><b>All other objects</b></td>
    <td>
      Anything that doesn't fall into one of the groups above is simply converted to `Strings` using the `toString()` method.
    </td>
    <td style={{backgroundColor: 'lightgreen', textAlign: 'center'}}><b>yes</b></td>
    <td style={{backgroundColor: 'salmon', textAlign: 'center'}}><b>no</b></td>
  </tr>
</table>

:::info
Serializers are designed to work on tree-shaped POJO models.
These are models where there are no referential loops (e.g. leaves with references to nodes, or nodes in one branch
referencing nodes in another branch).
There is a serializer setting `detectRecursions` to look for and handle these kinds of loops (by setting these
references to `null`) but it is not enabled by default since it introduces a moderate performance penalty.
:::

## POJOs convertible to/from Strings

A separate category exists for POJOs that can be converted to and from Strings.
These are used in places such as:

- Serializing of POJOs to Strings in the REST client API when no serializers are registered.
- Parsing of POJOs from Strings in the REST server API for `text/plain` requests where `text/plain` is not already mapped to an existing serializer.

As a general rule, all POJOs are converted to Strings using the `toString()` method.
However, there is one exception:

<tree>
<node-0><java-class><a href="https://docs.oracle.com/en/java/javase/17/docs/api/java.base/java/util/TimeZone.html" target="_blank">TimeZone</a></java-class> - Uses <java-method><a href="https://docs.oracle.com/en/java/javase/17/docs/api/java.base/util/TimeZone.html#getID()" target="_blank">TimeZone.getID()</a></java-method></node-0>
</tree>

POJOs are convertible from Strings using any of the following (matched in the specified order):

- Any of the following public static non-deprecated methods:
  - `create(String)`
  - `fromString(String)`
  - `fromValue(String)`
  - `valueOf(String)`
  - `parse(String)`
  - `parseString(String)`
  - `forName(String)`
  - `forString(String)`
- Has a public constructor that takes in a `String`.

Exceptions exist for the following classes:

<tree>
<node-0><java-class><a href="https://docs.oracle.com/en/java/javase/17/docs/api/java.base/java/util/TimeZone.html" target="_blank">TimeZone</a></java-class> - Uses <java-method><a href="https://docs.oracle.com/en/java/javase/17/docs/api/java.base/util/TimeZone.html#getTimeZone(String)" target="_blank">TimeZone.getTimeZone(String)</a></java-method></node-0>
<node-0><java-class><a href="https://docs.oracle.com/en/java/javase/17/docs/api/java.base/java/util/Locale.html" target="_blank">Locale</a></java-class> - Uses <java-method><a href="https://docs.oracle.com/en/java/javase/17/docs/api/java.base/util/Locale.html#forLanguageTag(String)" target="_blank">Locale.forLanguageTag(String)</a></java-method> after replacing `_` with `-`.</node-0>
<node-0><java-class><a href="https://docs.oracle.com/en/java/javase/17/docs/api/java.base/java/lang/Boolean.html" target="_blank">Boolean</a></java-class> - Blank and `"null"` are interpreted as `null` values.</node-0>
<node-0>Primitives (except for `void.class`) - Uses the primitive wrapper classes for instantiating from `Strings`.</node-0>
</tree>

## POJOs convertible to/from other types

POJOs are also converted to various other types in places such as the Open-API serializers and parsers.
In this section, the type being converted to will be referred to as `X`.

POJOs are considered convertible from X if it has any of the following (matched in the specified order):

- Any any of the following public static non-deprecated methods:
  - `create(X)`
  - `from*(X)`
- Has a public constructor that takes in an `X`.
- The X class has a public non-static no-arg non-deprecated method called `to*()`.

POJOs are considered convertible from X if any of the reverse of above are true.
