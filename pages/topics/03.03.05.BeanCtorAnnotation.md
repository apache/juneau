---
title: "@BeanCtor Annotation"
slug: BeanCtorAnnotation
---

The <a href="/site/apidocs/org/apache/juneau/commons/bean/BeanCtor.html" target="_blank">@BeanCtor</a> annotation is applied to bean constructors to map constructor arguments to property
names on beans with read-only properties.

Since method parameter names are lost during compilation, this annotation essentially redefines them so that
they are available at runtime and the framework can instantiate read-only beans during parsing.

This annotation lives in `juneau-commons` so the bean-modeling layer can describe constructor argument
mappings without depending on the marshalling layer.

:::note Migration
This annotation was previously named `@Beanc`. Usages of `@Beanc` should be updated to `@BeanCtor`
(in [org.apache.juneau.commons.bean](/site/apidocs/org/apache/juneau/commons/bean/package-summary.html)). This annotation can only be applied to constructors, and only one
constructor per class may carry it.
:::

## Basic Usage: `properties`

The definition of a read-only bean is a bean with properties with only getters, like shown below:

```java
// Our read-only bean.
public class Person {
    private final String name;
    private final int age;

    @BeanCtor(properties="name,age")
    public Person(String name, int age) {
        this.name = name;
        this.age = age;
    }

    // Read only properties.
    // Getters but no setters.

    public String getName() {
        return name;
    }

    public int getAge() {
        return age;
    }
}
```

```java
// Parsing into a read-only bean.
String json = "{name:'John Smith',age:45}";
Person person = Json.to(json, Person.class);
String name = person.getName();  // "John Smith"
int age = person.getAge();   // 45
```

Beans can also be defined with a combination of read-only and read-write properties.

## Using `@Name` Instead

The <a href="/site/apidocs/org/apache/juneau/commons/bean/Name.html" target="_blank">@Name</a> annotation can also
be used instead of `@BeanCtor(properties)` to label individual parameters:

```java
@BeanCtor
public Person(@Name("name") String name, @Name("age") int age) {
    this.name = name;
    this.age = age;
}
```

If neither `@BeanCtor(properties)` nor `@Name` is used to identify the bean property names, the framework
will try to use the parameter names if they are available in the bytecode.

## Java Records

For Java records, the canonical constructor and its property mappings are automatically detected, so this
annotation is not required. It can still be used to specify a non-canonical constructor if needed, for
example to provide default values for certain components:

```java
@BeanType(properties="name")
public record WithBeanCtor(String name, int age) {
    @BeanCtor(properties="name")
    public WithBeanCtor(String name) {
        this(name, 0);  // Default age to 0
    }
}
```

When using `@BeanCtor` with a non-canonical constructor on a record, use
<a href="/site/apidocs/org/apache/juneau/commons/bean/BeanType.html#properties()" target="_blank">@BeanType(properties)</a>
to limit the visible properties to match the constructor parameters.

:::info See Also
- [@BeanType Annotation](/docs/topics/BeanTypeAnnotation) - Class-level bean-modeling annotation.
- [@BeanProp Annotation](/docs/topics/BeanPropAnnotation) - Property-level bean-modeling annotation.
- [Java Records Support](/docs/topics/JavaRecordsSupport) - Full documentation on Java records support.
:::
