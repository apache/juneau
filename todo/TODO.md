# TODO

Live work plan for the Juneau 9.5.0 development cycle. Completed work is tracked separately in [`FINISHED.md`](FINISHED.md) — items move there when they land (and are removed from this file). The detailed per-TODO archives live alongside in `todo/FINISHED-<id>-<slug>.md`.

## Execution order (re-evaluated 2026-05-25)

**TODO-79 (`@Value` annotation + Spring Boot `application.yaml` bridge) is the next-in-flight item — pulled forward from Phase G** after a 2026-05-25 plan expansion added Phase 6 (internal adoption audit; scoped user-facing per OQA). TODO-78 (JSP) landed 2026-05-25; sibling view modules (TODO-82/83/84) remain queued behind TODO-96 (response-processor `prepend` mechanism, blocker for the deferred view-module Phase 5 integration matrix). Plans for TODO-71, TODO-82–84, TODO-85–87, and TODO-89 are fleshed out (2026-05-24). TODO-37 (agent instruction consolidation) remains parked. See [`FINISHED.md`](FINISHED.md) for the work already landed in this cycle.

### Live execution order (next, in dependency order)

**Phase A — `@Value` annotation + config bridge (next in flight):**

1. **TODO-79** — Juneau `@Value` annotation + `${...}` shortcut + Spring Boot `application.yaml` bridge for the Config API. Phases 1–4 implement the annotation + `BeanInstantiator` wiring + `${...}` tokenizer shortcut + `Config`/Spring `Environment` `PropertySource` auto-registration. Phase 5 docs/release-notes. **Phase 6 (added 2026-05-25)** runs an internal adoption audit, scoped **user-facing per OQA** (5–15 migrations cap; framework-internal pass deferred to TODO-92). Plan: `todo/TODO-79-value-annotation-config-bridge.md`.

**Phase B — view infrastructure** (landed 2026-05-25 as FINISHED-78: `View` interface + `juneau-rest-server-view-jsp` module + tests + docs; the real-container integration matrix is deferred behind TODO-96/97 and tracked separately).

**Phase C — view module siblings (parallelizable after TODO-96 lands; soft dep on the renderer-`prepend` mechanism that the deferred FINISHED-78 Phase 5 integration matrix needs to light up):**

2. **TODO-82** — Thymeleaf view module. Highest priority of the three (Spring Boot's default web view; biggest existing-user-base migration path). Plan: `todo/TODO-82-view-module-thymeleaf.md`.
3. **TODO-83** — Mustache view module. Smallest surface area, lowest risk. Plan: `todo/TODO-83-view-module-mustache.md`.
4. **TODO-84** — FreeMarker view module. Apache-family alignment bonus. Plan: `todo/TODO-84-view-module-freemarker.md`.

**Phase D — application showcase tier:**

5. **TODO-85** — `juneau-microservice-jetty-starter`. Standalone, no hard deps; can land any time after FINISHED-74/75/76/77 (which it bundles). Plan: `todo/TODO-85-microservice-jetty-starter.md`.
6. **TODO-86** — `juneau-petstore-jetty` sample app. Soft deps on TODO-85 (starter fallback to raw `juneau-microservice-jetty`), FINISHED-69 (auth fallback to `DenyAllGuard`), TODO-82 (preferred view engine: Thymeleaf, fallback to FINISHED-78 JSP). Defines the shared `juneau-petstore-core` module consumed by TODO-87. Plan: `todo/TODO-86-petstore-jetty-app.md`.
7. **TODO-87** — `juneau-petstore-springboot` sample app. HARD dep on `juneau-petstore-core` from TODO-86; should land alongside or immediately after TODO-86 as a coherent landing. Plan: `todo/TODO-87-petstore-springboot-app.md`.

> **Open question for TODO-86/87** (surfaced by planning worker): factor REST resource classes into a third sibling `juneau-petstore-rest` module so both sample apps consume identical resource classes? Worth resolving before either starts implementation.

**Phase E — quality-of-life follow-ons (parallelizable, any order, anytime):**

8. **TODO-89** — `RateLimitGuard.Storage.snapshot()` SPI + `BasicAdminResource` enrichment. Small (~30 LOC + 2 test files), closes a known carry-over from FINISHED-77 (`"buckets": []` placeholder). Plan: `todo/TODO-89-ratelimit-storage-snapshot-spi.md`.
9. **TODO-71** — Doc-site script + Docusaurus search swap. Mostly already done (planning worker found `juneau-docs/scripts/build-docs.py` + `.github/workflows/deploy-docs.yml.disabled` already in place); this TODO is now a cutover/cleanup + the Algolia → `@easyops-cn/docusaurus-search-local` swap. Plan: `todo/TODO-71-docs-site-script-search-swap.md`.
10. **TODO-88** — YAML parser buffer-underflow on large OpenAPI 3.1 documents. Latent parser bug in `juneau-marshall`; opportunistic. Plan file TBD.
11. **TODO-106** — FINISHED-archive follow-up/deferral sweep. Comb through every `todo/FINISHED-<id>-*.md` archive (~55 older + ~15 recent post-TODO-81 = ~70 files) looking at the "Known follow-ups / explicit deferrals" / "Carry-over" / "Open question" sections in each, plus inline `TODO: defer to TODO-<n>` style pointers in the archive narrative. Surface every item that was filed during execution but never promoted to a formal `[TODO-n]` bullet in `todo/TODO.md`. Output: one consolidated PR that adds bullets to `TODO.md` for each surfaced item, sized per item (trivial ones grouped under a single follow-on bullet, substantive ones promoted to standalone TODOs and `/todo expand`'d as appropriate). Avoid duplicates — many follow-ons were already explicitly tracked (TODO-92 from FINISHED-79 Phase 6, TODO-95 from FINISHED-79 perf cleanup, TODO-89 from FINISHED-77 admin enrichment, TODO-97/98 from FINISHED-78 phase 5 deferral, etc.) so the sweep is about catching the ones that fell through, not re-filing the ones already on the board. Independent; no hard deps. **Suggested before TODO-999 release-cleanup** so the deferral backlog is visible during 9.5.0 release-planning. Plan file TBD.

**Phase F — server-feature track (independent; can interleave anywhere):**

11. **TODO-67** — Observability (Micrometer + OpenTelemetry). Plan: `todo/TODO-67-observability-micrometer-otel.md`.
12. **TODO-68** — Bean Validation (Jakarta Validation 3.x). Plan: `todo/TODO-68-bean-validation-integration.md`.
13. **TODO-70** — `CompletableFuture<?>` return-type + virtual-thread per-request dispatch. Plan: `todo/TODO-70-async-completablefuture-virtual-threads.md`.
14. **TODO-92** — `@Value` framework-internal adoption pass — follow-on to TODO-79 Phase 6 (which is scoped user-facing only). Refactors framework-internal config readers (`Microservice` bootstrap, `RestContext.Builder` defaults, FINISHED-20's `DebugConfig` system-property defaults, FINISHED-69's `JwtTokenValidator` JWKS TTL, etc.) to flow through a `BeanInstantiator`-resolved seam so they can carry `@Value`. Bigger refactor than Phase 6; deliberately sequenced independently to keep TODO-79 reviewable. Plan file TBD — inventory of candidates is filed alongside TODO-79's landing per Phase 6's discovery-report acceptance criterion.
15. **TODO-95** — Per-RestContext `@Value("${cfg-key}")` resolution against `@Rest(config=...)` Configs — follow-on to FINISHED-79's perf-regression cleanup (the dropped `RestContext.build()` → `Settings.get().addSource(...)` bridge that leaked `ConfigPropertySource` instances into the process-wide `Settings` list because `MockRestClient` caches `RestContext` instances statically and never invokes `destroy()`). Re-introduce per-resource config-driven `@Value` resolution by routing through the per-RestContext `BeanStore` (or a `ThreadLocal<RestContext>` set during request scope) instead of mutating the process-wide `Settings` singleton. Plan file TBD.
16. **TODO-94a** — Auth filter framework. Pluggable servlet-filter chain in `JettyMicroservice` (URL-pattern-matched via `UrlPathMatcher`, multiple-overlapping auth options, role aggregation via `HttpServletRequestWrapper`, `@Guard` integration through `req.isUserInRole(...)`). Adds the missing `addFilter(...)` surface to `JettyServerComponent`. Reuses every FINISHED-69 type (`TokenValidator`, `ApiKeyStore`, `AuthenticationException`, `ClaimsPrincipal`, `@Auth`, `RestServerConstants.PRINCIPAL_ATTR`). HARD prereq for TODO-94b/c. Plan: `todo/TODO-94a-auth-filter-framework.md`.
17. **TODO-94b** — `juneau-rest-server-saml` module. SAML 2.0 assertion validation: `SamlAssertionValidator implements TokenValidator` + `SamlAuthFilter extends AuthFilter`, mandatory checks (signature / issuer / audience / NotBefore-NotOnOrAfter / subject confirmation), `opensaml` 5.x in `provided` scope. HARD dep on TODO-94a; soft dep on FINISHED-69 (SPI shape + `juneau-rest-server-jwt` packaging precedent). Plan: `todo/TODO-94b-saml-module.md`.
18. **TODO-94c** — `juneau-rest-server-oauth` module. OAuth 2.0 / OIDC integration: `OAuthIntrospectionValidator` (RFC 7662) + `OAuthFilter` + flow helpers (authorization-code-with-PKCE, client-credentials, resource-owner, refresh-token) + OIDC discovery; JDK `java.net.http.HttpClient` only; reuses FINISHED-69's `JwtTokenValidator` for JWT access tokens via `provided`-scope dep on `juneau-rest-server-jwt`. HARD dep on TODO-94a; soft dep on FINISHED-69. Plan: `todo/TODO-94c-oauth-module.md`.
19. **TODO-104** — Mixin path retrofit to use `pathToken()` normalization. Small focused follow-on to the FINISHED-102 / FINISHED-103 landing: wrap each FINISHED-101 SVL-configurable mixin path token in `#{pathToken(...)}` so user overrides like `"jsp"`, `"/jsp"`, `"/jsp/"`, `"/jsp/*"`, or `"jsp/*"` all resolve to the same clean `/jsp/*` mount instead of mangled forms (`//jsp/*`, `/jsp/*/*`). Touches all 10 SVL-configurable mixin classes (`BasicSwaggerResource`, `BasicSwaggerUiResource`, `BasicOpenApiResource`, `BasicRedocResource`, `BasicStaticFilesResource`, `BasicVersionResource`, `BasicEchoResource`, `BasicRouteIndexResource`, `BasicAdminResource`, `BasicJspResource`); each per-class `_SvlPathOverride_Test` extends with 5 input-form equivalence cases + 1 multi-segment case. Hard dep on TODO-102 now ✓ landed (`pathToken()` function shipped in the FINISHED-102 catalog). Plan: `todo/TODO-104-mixin-pathtoken-retrofit.md`.

> **Parallelization note for TODO-94b / TODO-94c**: parallelizable after TODO-94a lands; no inter-sibling deps (SAML and OAuth implementations don't depend on each other).

### Parked / unscheduled

- **TODO-37** — Agent instruction consolidation (unscoped).

### Release gate (run when cutting 9.5.0)

- **TODO-999** — Final pre-release cleanup pass (utility-method migration sweep + FQCN cleanup + full SonarQube scan + TODO-execution-vocabulary cleanup in code comments [`TODO-N` IDs, phase markers, `FINISHED-N` refs, OQA/RD/process narrative] + in-code `TODO`/`FIXME`/`XXX` marker resolution). **Marker ID `999` is deliberate** — this is the always-last item; renumbered out of the sequential range so it sorts last in any ID-ordered view and isn't confused for in-flight work. Plan file is a **living document** — add items as they surface during regular development by dropping `// TODO - Use <Helper>.<method>()`-style hint comments in code, then logging them in the plan's "Working notes" section for batch processing when the release branch opens. Plan: `todo/TODO-999-final-prerelease-cleanup.md`. **Do not start before all other in-flight TODOs above have landed.**

### Natural review seams

`@Value` + config bridge (TODO-79) → view siblings (TODO-82/83/84, gated on TODO-96 unblock) → application showcase (TODO-85/86/87) → quality-of-life (TODO-89 + TODO-71 + TODO-88) → server-feature track (TODO-67 + TODO-68 + TODO-70 + TODO-92 + TODO-95).

## Items

- [TODO-37] - Agent instruction consolidation.

- [TODO-67] Observability hooks — Micrometer + OpenTelemetry seams via `MethodExecStats`. See `todo/TODO-67-observability-micrometer-otel.md`.

- [TODO-68] Bean Validation (Jakarta Validation 3.x) integration on request beans. See `todo/TODO-68-bean-validation-integration.md`.

- [TODO-70] `CompletableFuture<?>` return-type support + optional virtual-thread per-request dispatch. See `todo/TODO-70-async-completablefuture-virtual-threads.md`.

- [TODO-71] Move doc site updates from a github hook to a script that gets executed locally. Change docusaurus search functionality to @easyops-cn/docusaurus-search-local. See `todo/TODO-71-docs-site-script-search-swap.md`.

- [TODO-79] Juneau `@Value` annotation + `${xxx}` shortcut in `VarResolver` + Spring Boot `application.yaml`/`application.properties` bridge for the Config API. Adds a declarative config-read annotation on bean fields, setters, and constructor parameters (analog to Spring's `@Value` but evaluated through Juneau's `Settings`/`VarResolver`/`Config` pipeline), a `${...}` tokenizer shortcut that resolves through the same `VarResolver` chain as `$P{...}`, and a `juneau-rest-server-springboot`-side `PropertySource` that bridges Spring's `Environment` into that chain so values defined in `application.yaml` reach native Juneau consumers without a second config file. Phase 6 (added 2026-05-25) runs an internal adoption audit — scoped user-facing only per OQA (5–15 migrations cap); framework-internal pass deferred to TODO-92. See `todo/TODO-79-value-annotation-config-bridge.md`.

- [TODO-82] Thymeleaf view module (`juneau-rest-server-view-thymeleaf`) — sibling to FINISHED-78's JSP module. New Maven module shipping `BasicThymeleafResource` mixin + `ThymeleafViewRenderer` + `ThymeleafView` impl of the `View` interface introduced by FINISHED-78. High priority since Thymeleaf is Spring Boot's default web view technology; large existing user base for Spring-Boot-on-Juneau migrations. Same engine-agnostic POM stance as FINISHED-78 (Option B): bridge module declares Thymeleaf core API in `provided` scope only; example module supplies the concrete `thymeleaf` + `thymeleaf-spring6` deps. **Soft prereq on TODO-96** (the response-processor `prepend` mechanism that FINISHED-78 deferred to TODO-96 — same Phase 5 integration constraint applies here). See `todo/TODO-82-view-module-thymeleaf.md`.

- [TODO-83] Mustache view module (`juneau-rest-server-view-mustache`) — sibling to FINISHED-78's JSP module. New Maven module shipping `BasicMustacheResource` mixin + `MustacheViewRenderer` + `MustacheView` impl of the `View` interface introduced by FINISHED-78. Logic-less templates; common choice for content authored by non-Java developers. Same engine-agnostic POM stance as FINISHED-78 (Option B): bridge declares the Mustache API in `provided` scope; example supplies the concrete impl (resolved decision: `com.github.spullara.mustache.java:compiler` over `jmustache`). **Soft prereq on TODO-96** (same constraint as TODO-82). See `todo/TODO-83-view-module-mustache.md`.

- [TODO-84] FreeMarker view module (`juneau-rest-server-view-freemarker`) — sibling to FINISHED-78's JSP module. New Maven module shipping `BasicFreemarkerResource` mixin + `FreemarkerViewRenderer` + `FreemarkerView` impl of the `View` interface introduced by FINISHED-78. Apache FreeMarker; widely used in admin consoles and reporting tools. Same engine-agnostic POM stance as FINISHED-78 (Option B): bridge declares `org.freemarker:freemarker` in `provided` scope; example supplies the runtime version. **Soft prereq on TODO-96** (same constraint as TODO-82). See `todo/TODO-84-view-module-freemarker.md`.

- [TODO-85] New `juneau-microservice-jetty-starter` module — zero-config quick-start for `JettyMicroservice`, conceptually analogous to Spring Boot's `spring-boot-starter-web`. Sits on top of the existing `juneau-microservice-jetty` runtime and bundles the dependency surface a new user typically wants (microservice-jetty + rest-server + the default marshall stack + sensible config defaults + a default `application.cfg`/`jetty.xml` baseline served from the JAR). Goal: `pom.xml` adds one dep, `main()` calls one factory, app comes up serving JSON/HTML over Jetty with a curated mixin set (API-docs + favicon + version) mounted by default. Mirrors the "you should not have to think about transitive deps to ship a hello-world Juneau service" ergonomic that Spring Boot solved on its side. See `todo/TODO-85-microservice-jetty-starter.md`.

- [TODO-86] New `juneau-petstore-jetty` sample application — a self-contained reference app deployed on top of `JettyMicroservice` (preferably via the TODO-85 starter once it lands) that exercises a wide cross-section of Juneau features so prospective users have one canonical place to read "how a real Juneau service is structured". Feature coverage: CRUD REST resources composed with the API-docs mixin pack (FINISHED-74), static-files mixin (FINISHED-75), convention endpoints (FINISHED-76), at least one ops/introspection mixin (FINISHED-77), at least one view-rendered HTML page (resolved decision: Thymeleaf via TODO-82 with JSP fallback via FINISHED-78), parent-chain mixin aggregation, `@Rest(paths=...)` runtime overrides (FINISHED-73), AuthN guards (FINISHED-69), and a non-trivial bean model with serialization round-trips across JSON/HTML/XML/URL-encoded/plain-text to showcase the marshall layer. Pet-store domain chosen for parity with the Swagger/OpenAPI canonical example so users can compare apples-to-apples against the spec. Defines the shared `juneau-petstore-core` Maven module also consumed by TODO-87. See `todo/TODO-86-petstore-jetty-app.md`.

- [TODO-87] New `juneau-petstore-springboot` sample application — identical domain + feature coverage as TODO-86, but deployed on Spring Boot via `BasicSpringRestServlet` + `ServletRegistrationBean` + `SpringBeanStore` (NOT `JuneauRestInitializer` — that class does not exist in the codebase; the plan file documents the correction). Mounted into a `@SpringBootApplication` so `mvn spring-boot:run` (or `java -jar`) brings up the same pet-store API with the same six API-docs URLs, same view-rendered pages, same guards, etc. Demonstrates the "byte-identical content across deployment modes" claim FINISHED-74 makes concrete in tests; also serves as the canonical reference for users migrating an existing Spring Boot app onto Juneau REST. Beans wired as `@Bean`s in a `@Configuration` class so the wiring style is recognizable to Spring devs. Shares the bean/domain model with TODO-86 via the `juneau-petstore-core` Maven module defined there. See `todo/TODO-87-petstore-springboot-app.md`.

- [TODO-89] `RateLimitGuard.Storage` snapshot SPI — surfaced during FINISHED-77's `BasicAdminResource` work. The `/admin/ratelimit` endpoint currently emits the rate-limit guard's static configuration only because `RateLimitGuard.Storage` has no read-side / snapshot operation; operators can see "what the rate-limit policy is" but not "which buckets are currently throttled and at what fill level". Add a `default Map<String, BucketState> snapshot()` to `RateLimitGuard.Storage` plus a new token-bucket-vocabulary `BucketState` record, override it in the bundled in-memory storage, and enrich `BasicAdminResource#getRateLimit(...)` to include live bucket state alongside configuration. Keep the SPI optional (default returns `Map.of()` so external Redis-backed storages keep compiling). Acceptance: `/admin/ratelimit` returns both config + live bucket state on the default storage. See `todo/TODO-89-ratelimit-storage-snapshot-spi.md`.

- [TODO-88] YAML parser buffer-underflow on large OpenAPI 3.1 documents — `OpenApiYamlRoundTrip_Test#c01` currently asserts against the small OpenAPI mount via `noInherit={"mixins"}` + `mixins=BasicOpenApiResource.class` because round-tripping the full post-FINISHED-74 `BasicRestServlet` mixin surface (six api-docs URLs, full schema set) through the YAML parser throws `java.io.IOException: Buffer underflow`. Surfaced during FINISHED-74's `apiFormat` removal but the root cause is a separate latent limitation in the YAML parser's buffer management on large docs, not a regression introduced by the mixin pack. Investigate the parser's read-buffer sizing and growth strategy (likely in `juneau-marshall`'s YAML parser core), reproduce with a focused parser-level test that bypasses REST entirely, fix the underflow, then remove the workaround from `OpenApiYamlRoundTrip_Test#c01`. Acceptance: full `BasicRestServlet` post-FINISHED-74 OpenAPI 3.1 doc round-trips JSON → YAML → JSON without `Buffer underflow`. Plan file TBD.

- [TODO-92] `@Value` framework-internal adoption pass — follow-on to TODO-79 Phase 6 (which is scoped user-facing per OQA 2026-05-25). Refactors framework-internal config readers that today hand-roll lookups in static initializers or un-injected constructors (notably `juneau-microservice/Microservice.java` bootstrap, `RestContext.Builder` defaults, FINISHED-20's `DebugConfig` system-property defaults, FINISHED-69's `JwtTokenValidator` JWKS TTL default) to route through a `BeanInstantiator`-resolved seam so they can carry the new `@Value` annotation. Deliberately a separate PR from TODO-79 — keeps TODO-79 reviewable and lets the framework-internal pass be sequenced independently. Inventory of candidates is filed alongside TODO-79's landing per Phase 6's discovery-report acceptance criterion. Plan file TBD.

- [TODO-95] Per-RestContext `@Value("${cfg-key}")` resolution against `@Rest(config=...)` Configs — follow-on to FINISHED-79's perf-regression cleanup (deferred from the dropped `RestContext.build()` → `Settings.get().addSource(...)` bridge). Re-introduce per-resource config-driven `@Value` resolution by routing through the per-RestContext `BeanStore` (or a `ThreadLocal<RestContext>` set during request scope) instead of mutating the process-wide `Settings` singleton. Architectural goal: a `BeanStore`-resolved `PropertySource`-equivalent that participates in `@Value`/`${...}` lookups within a resource's request-scope without leaking across resources or persisting beyond `RestContext.destroy()`. Re-enable any `@Disabled("TODO-95")` tests in `juneau-utest`. Plan file TBD.

- [TODO-94a] Auth filter framework — pluggable servlet-filter chain in `JettyMicroservice` (adds the missing `addFilter(...)` surface to `JettyServerComponent`) with `UrlPathMatcher`-driven matching, first-success short-circuit + role aggregation across overlapping filters, and `HttpServletRequestWrapper`-mediated visibility into `RestRequest.getUserPrincipal()` / `isUserInRole(...)` so downstream `RoleBasedRestGuard` / `@Auth Principal` consumers see filter-resolved identities without changes. Reuses every FINISHED-69 AuthN SPI verbatim. HARD prereq for TODO-94b/c. See `todo/TODO-94a-auth-filter-framework.md`.

- [TODO-94b] `juneau-rest-server-saml` — new opt-in Maven module shipping `SamlAssertionValidator implements TokenValidator` (FINISHED-69 SPI) + `SamlAuthFilter extends AuthFilter` (TODO-94a SPI) for consumer-side SAML 2.0 assertion validation (HTTP POST binding v1). Mandatory checks: signature, issuer, audience, NotBefore/NotOnOrAfter time-window, subject confirmation, signature-algorithm allowlist. Mirrors FINISHED-69's `juneau-rest-server-jwt` module isolation (`opensaml` 5.x declared `provided` scope; containment check confirms zero `org.opensaml` references in `juneau-rest-server`'s `dependency:tree`). HARD dep on TODO-94a; soft dep on FINISHED-69 (SPI + packaging precedent). See `todo/TODO-94b-saml-module.md`.

- [TODO-94c] `juneau-rest-server-oauth` — new opt-in Maven module shipping the resource-server side of OAuth 2.0 / OIDC: `OAuthIntrospectionValidator` (RFC 7662 opaque-token introspection with a bounded-LRU `TokenCache` SPI), `OAuthFilter extends AuthFilter` (TODO-94a SPI), a curated set of client-side flow helpers (authorization-code-with-PKCE per RFC 7636, client-credentials, resource-owner password-credentials with `@Deprecated`-since-9.5.0, refresh-token), and OIDC discovery via `/.well-known/openid-configuration`. For JWT access tokens, delegates verification to FINISHED-69's `JwtTokenValidator` via a `provided`-scope dep on `juneau-rest-server-jwt` (opaque-token-only consumers skip the JWT module entirely). All back-channel calls on the JDK `java.net.http.HttpClient`; no Nimbus / Spring Security at the module boundary. HARD dep on TODO-94a; soft dep on FINISHED-69. See `todo/TODO-94c-oauth-module.md`.

- [TODO-96] `ResponseProcessorList.Builder.prepend(...)` mechanism + view-renderer integration so `View`-returning `@RestOp` methods on a host class fire the registered renderer before `SerializedPojoProcessor`. Surfaced during FINISHED-78 phase 5 integration testing: the current `ResponseProcessorList.Builder.add(...)` chain is *append-only*, and the `DefaultConfig` defaults list (which ends with the catch-all `SerializedPojoProcessor`) is walked before any host-class `@Rest(responseProcessors=...)` contributions, so a custom renderer like `JspViewRenderer` can never run on host-class methods returning a `View`. Same issue blocks the sibling Thymeleaf / Mustache / FreeMarker bridges (TODO-82/83/84). Resolved decisions to revisit: should the mechanism be (a) a new `Builder.prepend(...)` API, (b) a `@Bean ResponseProcessorList` factory override pattern documented as the canonical user-facing escape hatch, or (c) a registered-renderer SPI that the framework auto-prepends when a `View`-typed return is detected anywhere in the routing table? Plan file TBD. **HARD prereq for TODO-97 and for the same phase in TODO-82/83/84.**

- [TODO-97] Real-container JSP integration tests for `juneau-rest-server-view-jsp` — FINISHED-78 phase 5 follow-up. Stand up the planned `BasicJspResource_JettyMicroservice_Test` + `BasicJspResource_Springboot_Test` matrix in `juneau-utest` (test JSP fixtures are pre-staged at `juneau-utest/src/test/resources/META-INF/resources/WEB-INF/views/{hello,raw}.jsp` and the test-scope deps — `juneau-rest-server-view-jsp`, `tomcat-embed-jasper`, `jetty-ee11-apache-jsp` — are already in `juneau-utest/pom.xml`). Two integration-environment knots to untie first: (1) **servlet-mapping ordering** — the existing `BasicSpringRestServlet` registration pattern `ServletRegistrationBean<Host>(servlet, "/*")` beats the embedded Tomcat / Jetty JSP servlet's `*.jsp` extension mapping at request dispatch, so `RequestDispatcher.forward(...)` to a `/WEB-INF/views/*.jsp` URL routes back through Juneau as a 404 instead of hitting the JSP engine; the workaround is to register Juneau on a more specific path or as the Servlet 3.0 default servlet, but both flavors need a recipe in the topic docs; (2) **response-processor ordering** — blocked by TODO-96 (host-class `@RestOp` methods returning a `View` never reach `JspViewRenderer` because `SerializedPojoProcessor` wins first). Acceptance: full three-flavor matrix (`MockRest` + `JettyMicroservice` + `Springboot`) plus a `_MultiBasePath_Test` and a `_NoEngine_Test` per the FINISHED-78 plan, all green on `scripts/test.py`. Plan file TBD. **HARD dep on TODO-96.**

- [TODO-98] Example modules `juneau-examples-rest-jetty-jsp` + `juneau-examples-rest-springboot-jsp` — FINISHED-78 phase 4 follow-up. Two end-to-end "hello world JSP" sample apps living under `juneau-examples/`, one per deployment style. Soft dep on TODO-97 (the integration-test work surfaces the exact servlet-mapping recipes the examples should ship with). Plan file TBD.

- [TODO-106] FINISHED-archive follow-up/deferral sweep — comb through every `todo/FINISHED-<id>-*.md` archive (~70 files post-TODO-103 landing) and surface "Known follow-ups / explicit deferrals / Carry-over / Open question" items that were filed during execution but never promoted to formal `[TODO-n]` bullets in `todo/TODO.md`. Many follow-ons are already explicitly tracked (TODO-92 ← FINISHED-79 Phase 6, TODO-95 ← FINISHED-79 perf cleanup, TODO-89 ← FINISHED-77 admin enrichment, TODO-97/98 ← FINISHED-78 phase 5 deferral, etc.) — this sweep is about catching the ones that fell through, NOT re-filing the ones already on the board. Output: one consolidated PR adding bullets to `TODO.md` for each surfaced item; trivial items grouped under a single follow-on bullet, substantive items promoted to standalone TODOs (and `/todo expand`'d when warranted). Independent; no hard deps. **Suggested before TODO-999 release-cleanup** so the deferral backlog is visible during 9.5.0 release-planning. Plan file TBD.

